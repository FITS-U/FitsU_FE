const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'vertical.pstatic.net', // 외부 이미지 도메인 추가
      'another-domain.com', // 다른 추가 도메인
    ],
  },
};

export default nextConfig;
